---
title: Installation
description: Getting started with portui
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";

# Installation

Getting started with portui

## 1. Install utilities

All components use [Tailwind CSS v4](https://tailwindcss.com/docs/installation/framework-guides/nextjs), so ensure it's installed in your project. Many components also use the `cn` utility function—install it with the following command:

<Tabs>
  <Tab label="bun" value="bun">
    <pre><code>bunx shadcn@latest add https://ui.portui.com/r/utils.json</code></pre>
  </Tab>
  <Tab label="npx" value="npx">
    <pre><code>npx shadcn@latest add https://ui.portui.com/r/utils.json</code></pre>
  </Tab>
  <Tab label="pnpm" value="pnpm">
    <pre><code>pnpm dlx shadcn@latest add https://ui.portui.com/r/utils.json</code></pre>
  </Tab>
</Tabs>